# /etc/conf.d/kea-dhcp6: config file for /etc/init.d/kea-dhcp6

# If you require more than one instance of dhcpd you can create symbolic
# links to dhcpd service like so
#   cd /etc/init.d
#   ln -s kea-dhcp6 kea-dhcp6.foo
#   cd ../conf.d
#   cp kea-dhcp6 kea-dhcp6.foo
# Now you can edit kea-dhcp6.foo and specify a different configuration file.

# Specify a configuration file - the default is /etc/kea/kea-dhcp6.conf
# KEA_DHCP4_CONF="/etc/kea/kea-dhcp6.conf"
