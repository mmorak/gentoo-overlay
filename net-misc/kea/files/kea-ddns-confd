# /etc/conf.d/kea-ddns: config file for /etc/init.d/kea-ddns

# If you require more than one instance of dhcpd you can create symbolic
# links to dhcpd service like so
#   cd /etc/init.d
#   ln -s kea-ddns kea-ddns.foo
#   cd ../conf.d
#   cp kea-ddns kea-ddns.foo
# Now you can edit kea-ddns.foo and specify a different configuration file.

# Specify a configuration file - the default is /etc/kea/kea-ddns.conf
# KEA_DHCP4_CONF="/etc/kea/kea-ddns.conf"
