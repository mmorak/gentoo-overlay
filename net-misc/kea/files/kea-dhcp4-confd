# /etc/conf.d/kea-dhcp4: config file for /etc/init.d/kea-dhcp4

# If you require more than one instance of dhcpd you can create symbolic
# links to dhcpd service like so
#   cd /etc/init.d
#   ln -s kea-dhcp4 kea-dhcp4.foo
#   cd ../conf.d
#   cp kea-dhcp4 kea-dhcp4.foo
# Now you can edit kea-dhcp4.foo and specify a different configuration file.

# Specify a configuration file - the default is /etc/kea/kea-dhcp4.conf
# KEA_DHCP4_CONF="/etc/kea/kea-dhcp4.conf"
